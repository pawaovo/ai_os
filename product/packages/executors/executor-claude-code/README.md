# Claude Code Executor

Placeholder package for the Claude Code Executor adapter.

V0.1 should map Claude Code process output and approval flow into `@ai-os/executor-protocol` and `@ai-os/kernel-events`. UI and Control Plane code must not consume Claude Code-native output directly.

