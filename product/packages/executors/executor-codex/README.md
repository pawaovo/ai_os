# Codex Executor

Placeholder package for the Codex Code Executor adapter.

V0.1 should map Codex app-server or CLI events into `@ai-os/executor-protocol` and `@ai-os/kernel-events`. UI and Control Plane code must not consume Codex-native events directly.

