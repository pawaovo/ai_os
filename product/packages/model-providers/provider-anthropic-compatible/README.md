# Anthropic-Compatible Provider

Placeholder package for Anthropic-compatible model provider integration.

V0.1 must support user supplied `baseUrl` and API key through the shared provider protocol. Do not add a real SDK dependency until `@ai-os/provider-protocol` is stable.

